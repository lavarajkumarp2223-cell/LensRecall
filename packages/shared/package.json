{
  "name": "@lensrecall/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Shared types, constants, and validation schemas for LensRecall",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --build"
  },
  "dependencies": {
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@lensrecall/config": "*",
    "typescript": "^5.7.3"
  }
}
