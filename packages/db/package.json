{
  "name": "@lensrecall/db",
  "version": "0.0.1",
  "private": true,
  "description": "Database schema, migrations, and client for LensRecall",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:seed": "tsx src/seeds/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.43.1",
    "postgres": "^3.4.5",
    "@neondatabase/serverless": "^0.10.4"
  },
  "devDependencies": {
    "@lensrecall/config": "*",
    "@lensrecall/shared": "*",
    "drizzle-kit": "^0.31.1",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  }
}
